/*
     Up until now, I just create the routes for the posts, Now lets move to the likes routes. 
*/

//Import express and router 
const express = require('express');
const router = express.Router();

//Import the database 
const db = require('../db');

